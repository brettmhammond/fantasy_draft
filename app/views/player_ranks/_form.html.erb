<%= form_for(@player_rank) do |f| %>
  <% if @player_rank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_rank.errors.count, "error") %> prohibited this player_rank from being saved:</h2>

      <ul>
      <% @player_rank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.text_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :position_rank %><br>
    <%= f.number_field :position_rank %>
  </div>
  <div class="field">
    <%= f.label :overall_rank %><br>
    <%= f.number_field :overall_rank %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
