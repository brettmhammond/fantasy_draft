<%= form_for(@fantasy_league) do |f| %>
  <% if @fantasy_league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fantasy_league.errors.count, "error") %> prohibited this fantasy_league from being saved:</h2>

      <ul>
      <% @fantasy_league.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
