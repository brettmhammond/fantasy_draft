<%= form_for(@bye) do |f| %>
  <% if @bye.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bye.errors.count, "error") %> prohibited this bye from being saved:</h2>

      <ul>
      <% @bye.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_id %><br>
    <%= f.text_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :week %><br>
    <%= f.number_field :week %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
